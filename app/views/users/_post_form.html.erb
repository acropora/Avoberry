<%= form_for @post, :url => new_post_user_path, :method => 'post', :remote => true do |f| %>
	<%= @post.content = nil %>
	<%= f.hidden_field :user_id, :value => user_id %>
	<%= f.label :new_post %><br />
	<%= f.text_area :content, :cols => 30, :rows => 4 %><br />
	<%= f.submit 'Submit' %>
<% end %>